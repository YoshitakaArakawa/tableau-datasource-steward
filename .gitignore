# 作業用ドラフト（commit しない）
work/
scripts/tableau_auth_pat.py

# 認証情報（実値は commit しない。テンプレートは .env.template）
.env

# OAuth トークンキャッシュ（生 access_token を含む）
.auth-cache/

# MCP 設定（PAT_VALUE を含む。実値は commit しない。テンプレートは .mcp.json.template）
.mcp.json

# Python
__pycache__/
*.pyc
