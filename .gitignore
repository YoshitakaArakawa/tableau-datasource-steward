# セッション作業物（規約は work/README.md。README のみ追跡）
work/*
!work/README.md
scripts/tableau_auth_pat.py

# 個人ローカル設定（commit しない）
.claude/settings.local.json

# 認証情報（実値は commit しない。テンプレートは .env.template）
.env

# OAuth トークンキャッシュ（生 access_token を含む）
.auth-cache/

.mcp.json

# Python
__pycache__/
*.pyc

# Tableau データソースアーティファクト（接続情報を含みうる。commit しない）
*.tdsx
*.tds
*.hyper
